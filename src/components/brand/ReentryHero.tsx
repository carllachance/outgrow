import { BrandHeader } from './BrandHeader';

export const ReentryHero = () => (
  <BrandHeader
    title="Welcome back."
    subtitle="No streak required."
    note="Pick one clear next step."
    kicker="Today"
  />
);
